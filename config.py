config = {
    # how sensitive motion detection is
    "motion_threshold": 4,

    # what is considered "big" motion
    "big_motion_threshold": 30,

    # how many still frames before motion ends
    "still_frame_threshold": 20,

    # when to stop recording a long motion
    "automatic_motion_cutoff": 200,
}
